diff --git a/kotlin-lsp.sh b/kotlin-lsp.sh
index 9a9a9a9..b4b4b4b 100755
--- a/kotlin-lsp.sh
+++ b/kotlin-lsp.sh
@@ -17,5 +17,7 @@
-if [[ "$OSTYPE" == "darwin"* ]]; then
-    LOCAL_JRE_PATH="$DIR/jre/Contents/Home"
-else
-    LOCAL_JRE_PATH="$DIR/jre"
-fi
+if [ -z "${LOCAL_JRE_PATH:-}" ]; then
+    if [[ "$OSTYPE" == "darwin"* ]]; then
+        LOCAL_JRE_PATH="$DIR/jre/Contents/Home"
+    else
+        LOCAL_JRE_PATH="$DIR/jre"
+    fi
+fi
